// Need add way to choose status IC or GB
// Need add way to choose topic category (Keyboard, Keycap, Switch, PCB, Other)

const NewTopic = () => {
  return (
    <form action="">
      <input
        type="text"
        placeholder="Type here"
        className="input input-bordered input-md w-full max-w-xs"
      />
    </form>
  );
};

export default NewTopic;
